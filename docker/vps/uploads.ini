; Largest app-level upload limit is SecureUploadService::MAX_PAYMENT_PROOF_KB (5MB) — these need
; headroom above that plus multipart/form overhead, and nginx's client_max_body_size must match.
upload_max_filesize = 10M
post_max_size = 12M
