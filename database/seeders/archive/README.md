# Archived seeders

These seeders populated demo automotive catalog data and are **not** run by default.

Use factories in tests or create data via the admin panel after `php artisan app:init-admin`.
